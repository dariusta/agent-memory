---
title: >-
    Hot — Working Memory
category: meta
summary: >-
    Fast-context scratchpad read first by wiki-query: recent activity, active threads, and key takeaways. Rewritten on every sync.
updated: 2026-07-01T04:22:28Z
---

# Hot — Working Memory

The first thing `wiki-query` reads. Keep it short and current.

## Recent Activity

- [2026-07-01] First sync of the memory system itself → new **[[agent-memory-wiki]]** project page (HEAD `b0c73d0`). This repo is both the vault (`wiki/`) and the **cross-agent auto-distill engine**: every coding agent (Claude/Codex/opencode/pi/Gemini) hooks its session-end/compaction to one guarded `bin/wiki-distill.sh`, which spawns a detached headless `claude -p` running the `wiki-update` skill, then self-commits `wiki/`. Two durable external-tool lessons pulled out as global skills: **[[opencode-transcript-storage]]** (opencode 1.17.x has **no per-session transcript file** — everything's in a SQLite `opencode.db`, no export CLI; reconstruct by joining `message`+`part` on `session_id`) and **[[claude-headless-p-hooks-dont-fire]]** (a headless `claude -p` run **didn't fire the claude-obsidian PostToolUse auto-commit hook** — so the wrap-script must do the commit itself and verify the end-state directly). Full per-agent wiring already lives in `docs/agent-hooks.md`; the wiki page captures the *why* + gotchas (transcript-gate to kill the ephemeral-run cost flood, per-session debounce keyed per session, terminal-vs-recurring events, Codex has no global hooks/session-end).
- [2026-07-01] Synced **[[iphone-control]]** again (Session 7, HEAD `eb41493`) — full end-to-end live campaign on Austin-hal: **22 of 31 flows PASS live**. Merged into [[social-app-automation-mechanics]] + [[ui-automation-matcher-cascade]]: the previously-*planned* **`--media_index=N`** camera-roll picker is now **implemented** across `instagram-post`/`-reel`/`-story` + `tiktok-post` (deterministic grid-cell tap + "Next" as pre-steps, so the agent's goal drops media selection). Key gotcha (`eb41493`): the **2026 IG composer is camera-first with the gallery as a bottom drawer** — the first cut *guessed* a 3-col full-screen grid (`yf 0.43`) and silently mis-tapped, **burning ~$90** before recalibration; real layout is **4-col, `xf=(col*2+1)/8`, first row `yf 0.696`, "Next" `yf 0.079`, and cell 0 = the camera tile (first real media = index 1)**. Two new reusable lessons: a **guessed coordinate is worse than none** (coordinate twin of "wrong CV template"), and a **test-runner time cap must be ≥ ~500s** or it kills escalation-heavy flows mid-recovery and reads as a failure. Also: analytics flows want a **`no_content` success-acceptor** (`instagram-analytics-post` fails only because the account has no posts).
- [2026-06-30] Synced **[[stratton-internal]]** — built a **Gallery page** (sidebar "Other" section) showing all produced videos. Durable lesson → new [[video-url-resolution]]: a produced video usually lives in the **`video_assets`** table (public `url` or private `storage_path`), **not** `video_tasks.final_video_url`; the first cut queried only that column and silently dropped most videos. Fix reuses the canonical **`apps/web/lib/final-video.ts`** and adds a batch `resolveFinalVideoUrls()` (N+1-free) next to the single-item resolver. Also noted the **auth-gated-preview boundary** (every dashboard route 302s to `/login` with no Supabase session, so headless visual QA is impossible).
## Active Threads

- **agent-memory-wiki** — the cross-agent auto-distill system is now itself documented ([[agent-memory-wiki]]). Wiring is in `docs/agent-hooks.md` + `bin/wiki-distill.sh`. Known limitations to keep in mind: Codex hooks are **per-project** (`.codex/hooks.json` in stratton-internal/kori/iphone-control/`~`; copy the file to add a repo) and fire per-turn (no true session-end); opencode/pi distill on compaction+idle, debounced; an opencode hook can **silently never fire** if opencode loads plugins from a different config dir than you installed into (a `~/.mavis/…` mirror was seen). Kill-switch: `touch ~/.wiki-distill.disabled`; logs at `.vault-meta/distill.log`.

- **iphone-control** — esp32farm rig validated live on "Austin-hal" (iPhone XR, iOS 26.5). Session-7 end-to-end campaign: **22/31 flows PASS live** (analytics, save, view/edit-profile, warmup, like, follow, comment, youtube-warmup). The **camera-roll thumbnail picker is now deterministic** (`--media_index` implemented + IG coords calibrated, `eb41493`) — media selection reaches the in-app editor reliably; the remaining brittle piece is the **LLM-driven publish/discard editor tail** (still hits the step cap). Other code-side open work: the **moving-feed profile-open** flows (tiktok-follow, view-profile ×2) still need pause-first + a **CV avatar** landmark; `instagram-comment` needs the region-tap focus fix; add a **`no_content` acceptor** to `instagram-analytics-post`; recalibrate the **TikTok** media grid (only IG was fixed); a dynamic rail detector remains on the wish-list. Cost-deferred (session hit ~$137 on escalation-heavy runs): instagram-switch-professional, youtube-post, instagram-switch-account (needs a 2nd IG account). Human open items: seed camera-roll media, capture per-device CV templates, add a 2nd IG account (login handoff), **rotate the Anthropic API key that leaked into the chat**, and **delete the TikTok Drafts:1 sensitive-credentials recording** left from a prior session.
- **kori** — now installs & runs on the VC iPhone 17 Pro (Release via `xcodebuild` + `devicectl`, since this Mac's Xcode-beta has no Simulator). A SceneDelegate was added to `AppDelegate.swift` to clear the iOS 26+ launch SIGTRAP. **Open discrepancy:** the earlier sim note claims the AppDelegate was *already* on UIScene, but the device session found it stock-legacy — recheck the committed `AppDelegate.swift`. Onboarding `social`/`notifications` steps still fire native iOS prompts invisible in dev. See [[kori-ios-build-run]].
- **stratton-internal** — staging shares the prod Trigger.dev worker/queue and prod Supabase DB. New **Gallery** page (`apps/web/app/(dashboard)/gallery/`) is live and is the first consumer of the batch video resolver in [[video-url-resolution]]. Open follow-ups the author flagged: whether the gallery should also include *posted* videos (`posts` table) or show **multiple cards per task** (every asset revision, not just the best cut) — currently one best-cut card per task with a resolvable video; and it's verified only by typecheck/biome (no live visual render — auth-gated).

## Key Takeaways

- **Reading a coding agent's own transcript is per-tool and full of traps.** Claude/Gemini give you a JSONL path via stdin; Codex/pi pass a JSONL path as an arg; **opencode gives you no file at all** — its 1.17.x sessions live only in a SQLite `opencode.db` with no export CLI, so you must join `message`+`part` on `session_id` to rebuild the text. And a headless `claude -p` **won't run your interactive/plugin hooks** (the claude-obsidian auto-commit didn't fire), so a wrap-script must perform+verify any side effect itself. → [[opencode-transcript-storage]], [[claude-headless-p-hooks-dont-fire]], [[agent-memory-wiki]]
- **Behavioral/identity realism beats motor micro-realism for anti-detection.** The biggest fleet tell is every device behaving identically + many devices on one egress IP — not swipe geometry. Prioritize per-account seeded identity, scheduling, action-mix, account aging, and per-device proxy/SIM. → [[behavioral-realism-anti-detection]]
- **Deterministic-first, agent-last UI automation.** Tap via a cascade `template → region → OCR → LLM agent`; mark fragile taps optional; a *wrong* CV template — **or a guessed hard-coded coordinate** — is worse than none (it mis-locks/mis-taps confidently; calibrate coords against a live screenshot). **The whole cascade — agent included — assumes a static frame: a continuously-moving target (an avatar on a playing feed) needs a *pause*, not more escalation.** → [[ui-automation-matcher-cascade]]
- **Three LLM-UI-agent failures that masquerade as "bad matching":** (1) the recovery agent silently hits a **too-low step cap** mid-recovery — a *budget* failure, not a matching one (raise it, make it env-overridable); (2) the agent's own **guard/safety sub-prompt** that lists "credentials/passwords/tokens/API keys" **trips the model's safety filter** and makes it refuse — describe the constraint behaviorally instead of listing hot-button nouns; (3) the **test runner's own time cap** (250–350s) kills escalation-heavy flows (>400s) mid-recovery before they emit a RESULT — set it **≥ ~500s**. And for a rote sequence the LLM keeps flubbing, add a **deterministic primitive** (`replace_text` for field edits, `--media_index` for the media grid) rather than a smarter prompt. → [[ui-automation-matcher-cascade]]
- **macOS 26 dropped the on-device Vision OCR models** (`Found bundles : { }`) — both legacy and modern Vision APIs fail; swap to a self-contained engine (RapidOCR/ONNX). → [[macos-vision-ocr-models-missing]]
- **`pymobiledevice3 dvt launch` doesn't wake the display** — DVT capture then reads a black frame and flows "time out" misleadingly. Wake first (`POST /home`); set Auto-Lock to Never. → [[dvt-launch-does-not-wake-display]]
- **2026 IG/TikTok specifics:** pause the playing feed before navigating (closed-loop pointer can't lock on motion); TikTok's account switcher is a *two-tap* sequence; IG Reels has no follow/favorite on the rail; **IG insights/analytics require a Professional (Creator) account** (and want a `no_content` acceptor for empty accounts); **posting needs pre-existing camera-roll media**, and the **2026 IG composer is camera-first with the gallery as a 4-column bottom drawer** (`xf=(col*2+1)/8`, first row `yf 0.696`, cell 0 = camera, first real media = index 1) — driven deterministically by `--media_index=N`. → [[social-app-automation-mechanics]]
- **Native iOS prompts are invisible during testing** — `StoreReview.requestReview()` is suppressed outside App Store/TestFlight; the notification dialog fires once per install. → [[ios-permission-review-prompts]]
- **iOS 26/27 SDK hard-crashes "scene declared but not adopted."** An app whose Info.plist has a `UIApplicationSceneManifest` but still runs the legacy AppDelegate window lifecycle (stock Expo SDK 54 / RN 0.81) SIGTRAPs at launch (`_UIApplicationEvaluateRuntimeIssueForNoSceneLifecycleAdoption`). Add a real SceneDelegate. → [[ios26-scene-lifecycle-launch-crash]]
- **Don't build Xcode app-extension projects inside iCloud-synced `~/Documents`** — iCloud stamps `com.apple.FinderInfo` on freshly-built `.appex` and codesign rejects it; it re-stamps mid-build so stripping fails. Point `-derivedDataPath` at `~/Library`. → [[icloud-synced-repo-breaks-codesign]]
- **Trigger.dev "TTL (10m) expired" = a job routed to the `dev` environment** (no persistent worker), chosen by the `TRIGGER_SECRET_KEY` prefix. → [[trigger-dev-environment-routing]]
- **"The video" for a stratton-internal task isn't one column.** A produced cut usually lives in the `video_assets` table (public `url` or signed private `storage_path`), not `video_tasks.final_video_url` — so any list/enumeration must resolve through the canonical `apps/web/lib/final-video.ts`, not a single-column query, or it silently drops most videos. Add a **batch resolver next to the single-item one** for list surfaces. → [[video-url-resolution]]
